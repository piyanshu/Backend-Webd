/* There is a problem that we have to everytime restarts the server 
when we change our code */
/* There is a package built on nodejs name nodemon it monitors on our project 
as soon as we change javascript file it automatically restarts the server */
// We can install this nodemon package globally so it is available on every project
// To download package go to terminal and run a command - npm install -g nodemon (g stands for global)
// To run a server use - nodemon index.js (index.js stands for file name)